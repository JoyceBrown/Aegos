# Aegos 3.6.18

## Problem groups

- Diagnostics are grouped into connection, subscription, node, DNS, TUN, system proxy, and firewall categories.
- Added instant category filters with per-category counts.
- Normal checks and actionable failures use the same diagnostic snapshot, avoiding conflicting page state.

## Acceptance

- The Stage 6 audit requires every planned category in both backend and UI contracts.
