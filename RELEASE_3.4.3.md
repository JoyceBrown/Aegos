# Aegos 3.4.3

Current-node IPv6 outlet checkpoint.

## Changes

- Added IPv6 outlet probing through IPv6-only services.
- IPv6 outlet failure is classified instead of being confused with IPv4 success.

## Verification

- `npm run audit:ipv6-dns-safety`
- `npm run check`

## Artifact

Source-only checkpoint. SHA-256: Source-only
