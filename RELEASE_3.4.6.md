# Aegos 3.4.6

DNS leak detection checkpoint.

## Changes

- Reused runtime DNS hardening report in the IPv6/DNS safety snapshot.
- DNS safety is shown as a separate status from IPv6 support.

## Verification

- `npm run audit:ipv6-dns-safety`
- `npm run check`

## Artifact

Source-only checkpoint. SHA-256: Source-only
