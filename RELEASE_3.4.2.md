# Aegos 3.4.2

Current-node IPv4 outlet checkpoint.

## Changes

- Added current-node IPv4 outlet detection through the active Aegos proxy path.
- Result is reported separately from cached landing IP.

## Verification

- `npm run audit:ipv6-dns-safety`
- `npm run check`

## Artifact

Source-only checkpoint. SHA-256: Source-only
